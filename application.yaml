spring:
  application:
  name: Ifraud

  # DATA BASE INFORMATION
  url:
    username:
      password:


#HIBERNATE ORM CONFIGURATION
jpa:
  hibernate:
    ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true